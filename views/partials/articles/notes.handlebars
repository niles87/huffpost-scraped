<div>
    {{#each note}}
    <h3 class="note-title">{{title}}</h3>
    <p class="note-body">{{body}}</p>
    <button class="delete" data-id="{{_id}}" data-note="{{[]}}"><i class="fas fa-times"></i></button>
    {{/each}}
</div>